This is the Print plug-in for the Gimp, version 3.1 (development).

To build and install:

./configure
make
make install

For developers, prior to running configure, you must

aclocal;autoconf;automake

